
// Slick slider to get things sliding. 

    //un comment below here
    jQuery(document).ready(function(){
       jQuery('#slider').slick({
			autoplay: true,
			autoplaySpeed: 9000,
			pauseOnFocus: false,
			pauseOnHover: false,
			dots: false,
			infinite: true,
			fade: true,
  			cssEase: 'linear'
       });
    })
